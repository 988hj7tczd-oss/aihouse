# 安全政策

## 受支持的版本

| 版本 | 支持状态 |
|------|---------|
| 最新 Release | ✅ 安全更新 |
| 历史版本 | ❌ 不提供安全更新 |

## 报告安全漏洞

**请勿公开提交安全相关的 Issue。**

请将漏洞报告发送至：**security@aihouse.dev**

我们承诺：
- **48 小时内**确认收到报告
- **7 天内**发布修复或给出修复时间线
- 修复后公开致谢（经报告者同意）

## 报告内容

请包含以下信息：
- 漏洞类型（XSS、SQL 注入、权限提升等）
- 复现步骤
- 受影响版本
- 潜在影响

## 安全最佳实践

### API Key 管理

```bash
# 不要将 API Key 写在代码或配置文件中
# 正确做法：
export DEEPSEEK_API_KEY="sk-xxx"
# 或写入 ~/.aihouse/.env（已被 .gitignore 排除）
```

### 网络安全

- AIHouse 后端默认只监听 `127.0.0.1:9800`（仅本地访问）
- 不要将后端端口暴露到公网
- 桌面端通过本地 API 通信，不经过外部网络

### 配置文件安全

- `~/.aihouse/config.yaml` 包含 API Key 引用，不要分享
- `~/.aihouse/.env` 包含明文 API Key，确保文件权限为 600

## 已知安全特性

- ✅ API Key 通过环境变量引用，不硬编码
- ✅ 配置文件被 .gitignore 排除
- ✅ 后端只监听本地地址
- ✅ 不收集任何用户数据
- ✅ 不经过第三方服务器
